[project]
name = "agentic-eda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.28",
    "langflow>=1.8.3",
    "langgraph>=1.0.1",
    "langsmith>=0.7.22",
]
